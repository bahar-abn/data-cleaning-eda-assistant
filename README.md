📊 دستیار هوشمند پاکسازی داده و EDA
🚀 معرفی پروژه
دستیار هوشمند پاکسازی داده و EDA یک سیستم کامل و حرفه‌ای برای تحلیل خودکار داده‌های خام، پاکسازی هوشمند، مصورسازی پیشرفته و تولید گزارش‌های تحلیلی جامع است. این پروژه با معماری ماژولار و رابط کاربری گرافیکی زیبا، تمام مراحل تحلیل داده را از صفر تا صد خودکار می‌کند.

✨ قابلیت‌های کلیدی
📂 بارگذاری داده
پشتیبانی از فرمت‌های CSV, Excel, JSON

تشخیص خودکار فرمت فایل

اعتبارسنجی حجم و پسوند فایل

نمایش پیش‌نمایش داده‌ها

گزارش حافظه مصرفی

🧹 پاکسازی هوشمند داده
تشخیص و مدیریت مقادیر گمشده:

حذف ستون‌های با بیش از ۵۰% مقدار گمشده

پرکردن مقادیر عددی با میانگین/میانه/مد

پرکردن مقادیر دسته‌بندی با مد/مقدار ثابت

شناسایی و مدیریت داده‌های پرت:

روش IQR با ضریب قابل تنظیم

روش Z-Score با آستانه قابل تنظیم

گزینه‌های حذف، محدودسازی (Capping) یا Winsorize

مدیریت داده‌های تکراری:

شناسایی و حذف رکوردهای تکراری

نگهداری اولین/آخرین رکورد

بهینه‌سازی خودکار:

کاهش سایز داده‌ها

تبدیل ستون‌های متنی به categorical

کاهش مموری مصرفی تا ۷۰%

📊 تحلیل اکتشافی داده (EDA)
آمار توصیفی کامل:

میانگین، میانه، مد، واریانس

چولگی، کشیدگی

چندک‌ها و دامنه بین چارکی

تحلیل همبستگی:

محاسبه ماتریس همبستگی (Pearson, Spearman, Kendall)

شناسایی همبستگی‌های قوی

تحلیل روابط خطی

تحلیل توزیع:

آزمون‌های نرمال بودن (Shapiro-Wilk, D'Agostino)

تحلیل چولگی و کشیدگی

شناسایی توزیع‌های غیرنرمال

تحلیل ستون‌های دسته‌بندی:

فراوانی مقادیر

آنتروپی و تنوع

شناسایی مقادیر نادر

🎨 مصورسازی پیشرفته
هیستوگرام: نمایش توزیع داده‌ها با منحنی KDE

باکس پلات: شناسایی داده‌های پرت

هیتمپ همبستگی: نمایش ماتریس همبستگی

نمودار پراکندگی: نمایش روابط بین متغیرها

نمودار ستونی: نمایش داده‌های دسته‌بندی

داشبورد خودکار: تولید مجموعه نمودارهای هوشمند

📄 گزارش‌گیری خودکار
HTML Report: گزارش زیبا و تعاملی

JSON Report: داده‌های ساختاریافته

Text Report: گزارش متنی ساده

بینش‌های هوشمند: استخراج خودکار insights

ذخیره خودکار: ذخیره در پوشه outputs/reports

💻 رابط کاربری Streamlit
طراحی واکنش‌گرا (Responsive)

پشتیبانی کامل از زبان فارسی

تم مدرن و حرفه‌ای

تب‌بندی منظم

کارت‌های آماری زیبا

باکس‌های بینش

انیمیشن‌های روان

🏗 معماری پروژه
text
data-cleaning-eda-assistant/
│
├── 📁 src/                    # هسته اصلی پروژه
│   ├── __init__.py           #初始化 پکیج
│   ├── data_loader.py        # بارگذاری داده
│   ├── data_cleaner.py       # پاکسازی داده
│   ├── eda_analyzer.py       # تحلیل EDA
│   ├── visualizer.py         # مصورسازی
│   ├── report_generator.py   # تولید گزارش
│   └── utils.py              # توابع کمکی
│
├── 📁 streamlit_app/         # رابط کاربری
│   ├── __init__.py
│   ├── app.py               # برنامه اصلی
│   ├── components.py        # کامپوننت‌های UI
│   └── styles.py           # استایل‌های CSS
│
├── 📁 tests/                 # تست‌های واحد
│   ├── test_data_loader.py
│   ├── test_data_cleaner.py
│   └── test_eda_analyzer.py
│
├── 📁 notebooks/            # نوتبوک‌های Jupyter
│   └── example_analysis.ipynb
│
├── 📁 data/                 # داده‌ها
│   ├── 📁 raw/             # داده‌های خام
│   ├── 📁 processed/       # داده‌های پردازش شده
│   └── sample_data.csv     # نمونه داده
│
├── 📁 outputs/             # خروجی‌ها
│   ├── 📁 reports/        # گزارش‌های تولید شده
│   ├── 📁 figures/        # نمودارهای ذخیره شده
│   └── 📁 logs/           # فایل‌های لاگ
│
├── 📁 config/             # تنظیمات
│   └── config.yaml       # فایل پیکربندی
│
├── requirements.txt      # وابستگی‌ها
├── README.md            # مستندات
├── setup.py            # نصب پکیج
└── .gitignore         # فایل‌های ignore
💻 تکنولوژی‌های استفاده شده
دسته‌بندی	کتابخانه‌ها	کاربرد
پردازش داده	Pandas, NumPy	دستکاری و تحلیل داده
مصورسازی	Plotly, Matplotlib, Seaborn	رسم نمودارهای تعاملی و ایستا
رابط کاربری	Streamlit	برنامه وب تعاملی
تحلیل آماری	SciPy, Scikit-learn	آزمون‌های آماری، تشخیص outlier
گزارش‌گیری	Jinja2, PyYAML	تولید HTML، مدیریت تنظیمات
توسعه	Jupyter, Pytest, Black	توسعه، تست، فرمت کردن کد
🚀 راه‌اندازی و نصب
پیش‌نیازها
Python 3.8 یا بالاتر

pip (مدیریت پکیج پایتون)

Git (اختیاری)

مراحل نصب
bash
# 1. کلون کردن مخزن
git clone https://github.com/yourusername/data-cleaning-eda-assistant.git
cd data-cleaning-eda-assistant

# 2. ایجاد محیط مجازی
# لینوکس/مک:
python3 -m venv venv
source venv/bin/activate

# ویندوز:
python -m venv venv
venv\Scripts\activate

# 3. نصب وابستگی‌ها
pip install --upgrade pip
pip install -r requirements.txt

# 4. نصب پکیج پروژه
pip install -e .

# 5. ایجاد پوشه‌های مورد نیاز
mkdir -p data/raw data/processed outputs/reports outputs/figures outputs/logs config

# 6. ایجاد فایل تنظیمات
cp config/config.yaml.example config/config.yaml

# 7. اجرای برنامه
streamlit run streamlit_app/app.py
📖 راهنمای استفاده
🎯 روش اول: استفاده از رابط کاربری Streamlit (توصیه شده)
bash
streamlit run streamlit_app/app.py
سپس مراحل زیر را دنبال کنید:

آپلود فایل: از منوی کناری، فایل داده خود را آپلود کنید

پیش‌نمایش: در تب اول، اطلاعات کلی و پیش‌نمایش داده را ببینید

پاکسازی: در تب دوم، دکمه "اجرای پاکسازی خودکار" را بزنید

تحلیل: در تب سوم، "اجرای تحلیل کامل" را کلیک کنید

مصورسازی: در تب چهارم، داشبورد تصویری را مشاهده کنید

گزارش: در تب پنجم، گزارش را دانلود کنید

📈 عملکرد و بهینه‌سازی
حجم داده	زمان بارگذاری	زمان پاکسازی	زمان تحلیل	حافظه مصرفی
1 MB	< 1 ثانیه	< 1 ثانیه	< 2 ثانیه	~10 MB
100 MB	2-3 ثانیه	3-5 ثانیه	5-8 ثانیه	~150 MB
1 GB	15-20 ثانیه	20-30 ثانیه	30-45 ثانیه	~1.2 GB

